[project]
name = 'Decentralized-Shared-Mobility-Infrastructure'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.charging-station-registry]
path = 'contracts/charging-station-registry.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
